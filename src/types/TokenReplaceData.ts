/**
 * Represents an object mapping key tokens to string values,
 * where the token keys will be replaced with the provided
 * value in the source string
 */
type TokenReplaceData = { [key: string]: string };
